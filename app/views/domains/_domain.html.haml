%tr
  %td= link_to domain.name, [:edit,domain]
  %td= emails_badge(domain)
  %td= link_to raw("#{image_tag("domain-delete.png")} Delete"), domain_path(domain), :confirm => "Are you sure you want to delete the domain #{domain.name}? Also all emails, that belongs to this domain will be deleted", :method => :delete, :class => "btn btn-danger pull-right"
  