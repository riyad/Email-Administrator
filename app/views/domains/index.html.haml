.row-fluid
  .span6
    %table#domains.table.table-striped
      %thead
        %tr
          %th Domain
          %th
          %th
      %tbody
        = render @domains

    %p= link_to raw("#{image_tag("domain-add.png")} Add Domain"), new_domain_path, :class => "btn btn-success float=left"
