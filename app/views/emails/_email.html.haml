%tr{ :class => active_class(email) }
  %td= email.email
  %td= form_date_for_expires_on(email.expires_on)
  %td= last_activity_for(email)
  %td= admin_badge(email)
  %td= receive_badge(email)
  %td= send_badge(email)
  %td= forwards_badge(email)
  %td= link_to "Edit", [:edit,email], :class => 'btn'
