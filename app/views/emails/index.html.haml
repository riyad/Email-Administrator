%p
  = link_to new_email_path, :class => "btn btn-success" do
    = image_tag("email-add.png")
    Add Email

= form_tag emails_path, :method => 'get', :id => "emails_search", :class => "form-search" do
  .pull-right
    = text_field_tag :search, params[:search], :placeholder => "Search ...", :class => "search-query"

  #emails
    = render 'emails'
