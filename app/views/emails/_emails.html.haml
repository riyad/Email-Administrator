= hidden_field_tag :direction, params[:direction]
= hidden_field_tag :sort, params[:sort]

= page_navigation_links @emails

%table.table.table-striped
  %thead
    %tr
      %th= sortable "email"
      %th= sortable "expires_on"
      %th= sortable "last_activity_on", "Last Activity"
      %th{:colspan => 4}
      %th
  %tbody
    =render @emails

= page_navigation_links @emails
